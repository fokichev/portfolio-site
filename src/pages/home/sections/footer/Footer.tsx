import './Footer.scss';

const Footer = ({ id }: { id: string }) => {
    return (
        <div className='footer-container' id={id}>
            Footer placeholder
        </div>
    )
}

export { Footer }