import './Portfolio.scss';

const Portfolio = ({ id }: { id: string }) => {
    return (
        <div className='portfolio-container margin-content' id={id}>
            Portfolio placeholder
        </div>
    )
}

export { Portfolio }